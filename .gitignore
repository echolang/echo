CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
.cmake

# compiled module artifacts. The default store is `.echo` beside each manifest, so a library's cache lives
# with the code it belongs to - deleting a checkout deletes its cache, and no two projects share one
.echo/
