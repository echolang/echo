namespace std::math;

/**
 * Math functions that lower to a single LLVM intrinsic.
 *
 * every function here is declared twice, once for `float` (32 bit) and once for `float64`. the two
 * are a plain overload set: the argument type picks the candidate and each lowers to its own
 * `.f32` / `.f64` mangling, so single-precision math never round-trips through a double
 *
 * note that a float literal is double precision unless it ends in `f`, so `std::math::sqrt(2.0)` is the
 * float64 overload and `std::math::sqrt(2.0f)` the float one
 */

// ----------------------------------------------------------------------------
// trigonometry
// ----------------------------------------------------------------------------

/**
 * Returns the sine of a given value.
 */
#[inline]
#[intrinsic: "llvm.sin"]
public function sin(float $x) : float;

#[inline]
#[intrinsic: "llvm.sin"]
public function sin(float64 $x) : float64;

/**
 * Returns the cosine of a given value.
 */
#[inline]
#[intrinsic: "llvm.cos"]
public function cos(float $x) : float;

#[inline]
#[intrinsic: "llvm.cos"]
public function cos(float64 $x) : float64;

/**
 * Returns the tangent of a given value.
 */
#[inline]
#[intrinsic: "llvm.tan"]
public function tan(float $x) : float;

#[inline]
#[intrinsic: "llvm.tan"]
public function tan(float64 $x) : float64;

/**
 * Returns the arc sine of a given value, in radians.
 */
#[inline]
#[intrinsic: "llvm.asin"]
public function asin(float $x) : float;

#[inline]
#[intrinsic: "llvm.asin"]
public function asin(float64 $x) : float64;

/**
 * Returns the arc cosine of a given value, in radians.
 */
#[inline]
#[intrinsic: "llvm.acos"]
public function acos(float $x) : float;

#[inline]
#[intrinsic: "llvm.acos"]
public function acos(float64 $x) : float64;

/**
 * Returns the arc tangent of a given value, in radians.
 */
#[inline]
#[intrinsic: "llvm.atan"]
public function atan(float $x) : float;

#[inline]
#[intrinsic: "llvm.atan"]
public function atan(float64 $x) : float64;

/**
 * Returns the arc tangent of $y / $x, using the signs of both to pick the quadrant.
 */
#[inline]
#[intrinsic: "llvm.atan2"]
public function atan2(float $y, float $x) : float;

#[inline]
#[intrinsic: "llvm.atan2"]
public function atan2(float64 $y, float64 $x) : float64;

/**
 * Returns the hyperbolic sine of a given value.
 */
#[inline]
#[intrinsic: "llvm.sinh"]
public function sinh(float $x) : float;

#[inline]
#[intrinsic: "llvm.sinh"]
public function sinh(float64 $x) : float64;

/**
 * Returns the hyperbolic cosine of a given value.
 */
#[inline]
#[intrinsic: "llvm.cosh"]
public function cosh(float $x) : float;

#[inline]
#[intrinsic: "llvm.cosh"]
public function cosh(float64 $x) : float64;

/**
 * Returns the hyperbolic tangent of a given value.
 */
#[inline]
#[intrinsic: "llvm.tanh"]
public function tanh(float $x) : float;

#[inline]
#[intrinsic: "llvm.tanh"]
public function tanh(float64 $x) : float64;

// ----------------------------------------------------------------------------
// roots, powers, exponentials and logarithms
// ----------------------------------------------------------------------------

/**
 * Returns the square root of a given value.
 */
#[inline]
#[intrinsic: "llvm.sqrt"]
public function sqrt(float $x) : float;

#[inline]
#[intrinsic: "llvm.sqrt"]
public function sqrt(float64 $x) : float64;

/**
 * Returns $base raised to the power of $exponent.
 */
#[inline]
#[intrinsic: "llvm.pow"]
public function pow(float $base, float $exponent) : float;

#[inline]
#[intrinsic: "llvm.pow"]
public function pow(float64 $base, float64 $exponent) : float64;

/**
 * Returns e raised to the power of a given value.
 */
#[inline]
#[intrinsic: "llvm.exp"]
public function exp(float $x) : float;

#[inline]
#[intrinsic: "llvm.exp"]
public function exp(float64 $x) : float64;

/**
 * Returns 2 raised to the power of a given value.
 */
#[inline]
#[intrinsic: "llvm.exp2"]
public function exp2(float $x) : float;

#[inline]
#[intrinsic: "llvm.exp2"]
public function exp2(float64 $x) : float64;

/**
 * Returns 10 raised to the power of a given value.
 */
#[inline]
#[intrinsic: "llvm.exp10"]
public function exp10(float $x) : float;

#[inline]
#[intrinsic: "llvm.exp10"]
public function exp10(float64 $x) : float64;

/**
 * Returns the natural logarithm of a given value.
 */
#[inline]
#[intrinsic: "llvm.log"]
public function log(float $x) : float;

#[inline]
#[intrinsic: "llvm.log"]
public function log(float64 $x) : float64;

/**
 * Returns the base 2 logarithm of a given value.
 */
#[inline]
#[intrinsic: "llvm.log2"]
public function log2(float $x) : float;

#[inline]
#[intrinsic: "llvm.log2"]
public function log2(float64 $x) : float64;

/**
 * Returns the base 10 logarithm of a given value.
 */
#[inline]
#[intrinsic: "llvm.log10"]
public function log10(float $x) : float;

#[inline]
#[intrinsic: "llvm.log10"]
public function log10(float64 $x) : float64;

// ----------------------------------------------------------------------------
// rounding
// ----------------------------------------------------------------------------

/**
 * Returns the largest integral value not greater than a given value.
 */
#[inline]
#[intrinsic: "llvm.floor"]
public function floor(float $x) : float;

#[inline]
#[intrinsic: "llvm.floor"]
public function floor(float64 $x) : float64;

/**
 * Returns the smallest integral value not less than a given value.
 */
#[inline]
#[intrinsic: "llvm.ceil"]
public function ceil(float $x) : float;

#[inline]
#[intrinsic: "llvm.ceil"]
public function ceil(float64 $x) : float64;

/**
 * Returns a given value with its fractional part discarded, rounding toward zero.
 */
#[inline]
#[intrinsic: "llvm.trunc"]
public function trunc(float $x) : float;

#[inline]
#[intrinsic: "llvm.trunc"]
public function trunc(float64 $x) : float64;

/**
 * Returns the nearest integral value, rounding halfway cases away from zero.
 */
#[inline]
#[intrinsic: "llvm.round"]
public function round(float $x) : float;

#[inline]
#[intrinsic: "llvm.round"]
public function round(float64 $x) : float64;

/**
 * Returns the nearest integral value, rounding halfway cases to the even neighbour.
 *
 * this is banker's rounding, and it differs from round() on exact halves: round(2.5) is 3, while
 * roundeven(2.5) is 2
 */
#[inline]
#[intrinsic: "llvm.roundeven"]
public function roundeven(float $x) : float;

#[inline]
#[intrinsic: "llvm.roundeven"]
public function roundeven(float64 $x) : float64;

/**
 * Returns the nearest integral value, according to the current rounding mode.
 */
#[inline]
#[intrinsic: "llvm.rint"]
public function rint(float $x) : float;

#[inline]
#[intrinsic: "llvm.rint"]
public function rint(float64 $x) : float64;

// ----------------------------------------------------------------------------
// sign
// ----------------------------------------------------------------------------

/**
 * Returns the absolute value of a given value.
 *
 * these two sit alongside the generic abs<T> in functions.eco, and the overload set sorts it out:
 * a float argument matches one of these exactly, while an integer one instantiates the template
 */
#[inline]
#[intrinsic: "llvm.fabs"]
public function abs(float $x) : float;

#[inline]
#[intrinsic: "llvm.fabs"]
public function abs(float64 $x) : float64;

/**
 * Returns $magnitude with the sign of $sign.
 */
#[inline]
#[intrinsic: "llvm.copysign"]
public function copysign(float $magnitude, float $sign) : float;

#[inline]
#[intrinsic: "llvm.copysign"]
public function copysign(float64 $magnitude, float64 $sign) : float64;

// ----------------------------------------------------------------------------
// minimum, maximum and fused multiply-add
// ----------------------------------------------------------------------------

/**
 * Returns the smaller of two values.
 *
 * signed and unsigned integers need different instructions, so the integer overloads are spelled
 * out rather than generic. a mixed-signedness call has no exact candidate and is reported as such,
 * which is the honest answer - cast the operands to one type first
 */
#[inline]
#[intrinsic: "llvm.minnum"]
public function min(float $a, float $b) : float;

#[inline]
#[intrinsic: "llvm.minnum"]
public function min(float64 $a, float64 $b) : float64;

#[inline]
#[intrinsic: "llvm.smin"]
public function min(int32 $a, int32 $b) : int32;

#[inline]
#[intrinsic: "llvm.smin"]
public function min(int64 $a, int64 $b) : int64;

#[inline]
#[intrinsic: "llvm.umin"]
public function min(uint32 $a, uint32 $b) : uint32;

#[inline]
#[intrinsic: "llvm.umin"]
public function min(uint64 $a, uint64 $b) : uint64;

/**
 * Returns the larger of two values.
 */
#[inline]
#[intrinsic: "llvm.maxnum"]
public function max(float $a, float $b) : float;

#[inline]
#[intrinsic: "llvm.maxnum"]
public function max(float64 $a, float64 $b) : float64;

#[inline]
#[intrinsic: "llvm.smax"]
public function max(int32 $a, int32 $b) : int32;

#[inline]
#[intrinsic: "llvm.smax"]
public function max(int64 $a, int64 $b) : int64;

#[inline]
#[intrinsic: "llvm.umax"]
public function max(uint32 $a, uint32 $b) : uint32;

#[inline]
#[intrinsic: "llvm.umax"]
public function max(uint64 $a, uint64 $b) : uint64;

/**
 * Returns $a * $b + $c, computed with a single rounding.
 */
#[inline]
#[intrinsic: "llvm.fma"]
public function fma(float $a, float $b, float $c) : float;

#[inline]
#[intrinsic: "llvm.fma"]
public function fma(float64 $a, float64 $b, float64 $c) : float64;
