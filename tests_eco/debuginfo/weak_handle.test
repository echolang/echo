mode: build
flags: -g

--- OUT --->
1
--- UNIT_IR --->
# compiling this at all with `-g` is the assertion: the weak arm used to recurse on itself until
# SIGSEGV, so there was no IR to check. the rest says the class and the weak local were described
CHECK: #dbg_declare(

CHECK: !DICompositeType(tag: DW_TAG_structure_type, name: "Node"

CHECK: !DILocalVariable(name: "w"
