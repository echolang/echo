// join puts DS between two components, and does not double a separator
// the directory already ended with.

echo std::env::join('foo', 'bar');
echo std::env::join('foo/', 'bar');
echo std::env::join('', 'bar');
echo std::env::join('foo', '');
