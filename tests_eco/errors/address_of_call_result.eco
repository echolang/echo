// `&get()` is a function-ref plus a postfix call. assigning that int32 to a
// borrow needs a place, which a call is not - the declaration auto-borrows
// the call result into a temporary and then refuses, because that slot dies
// at the end of the statement
function get() : int32
{
    return 5;
}

int32& $r = &get();
