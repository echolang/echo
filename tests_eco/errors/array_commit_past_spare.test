expect: fail

# the line is the assert inside array.eco, not the call site. update the number if that
# assert moves; the claim is that the location names the file the assert was written in

--- OUT --->
assertion failed: commit past the end of the spare
  at array.eco:361
