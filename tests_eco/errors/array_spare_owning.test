expect: fail

# die is baked into the method, so the location names array.eco. update the number if
# the die moves; the claim is that an owning element is refused rather than committed

--- OUT --->
fatal error: spare() needs elements that are just bytes - C cannot construct an owning T
  at array.eco:346
