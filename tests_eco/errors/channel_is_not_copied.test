expect: fail

--- OUT --->
[error] CannotCopy  channel_is_not_copied.eco:7:29

  'std::thread::channel<int32>' is unique: exactly one value may name its storage, so it is moved and never copied.

  6 | thread::channel<int32> $a = thread::channel<int32>();
  7 | thread::channel<int32> $b = $a;
    :                             ^^
  8 | 

  help: Write 'mv $a' to transfer it. Or take a borrow ('std::thread::channel<int32>&') if it is only being read.

1 error, nothing was compiled.
