// an address only compares against another address - a different rule from
// null_compare_value.eco, where the wrong side was read. arithmetic still mixes freely, since
// `$p:$ + 1` offsets an address rather than comparing it
$a = 1;
ptr<int> $p = &$a;
echo ($p:$ == 0);
