expect: fail

--- OUT --->
[error] const_if_needs_a_layout.eco:3:1

  'size_of' is answered from the target's layout, which the compiler only knows once it is emitting code - so it cannot decide a 'const' expression.

  2 | // cannot decide a compile-time branch. a refusal rather than a not-yet: no round is going to answer it.
  3 | const if (mem::size<int32>() > 2) {
    : ^^^^^
  4 |     echo 1;

1 error, nothing was compiled.
