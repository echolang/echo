// a destructor returns nothing, so a `: type` is rejected outright rather than checked against void

struct Buffer
{
    ptr<uint8> $data;

    destructor() : void
    {
        mem::free($this->data);
    }
}
