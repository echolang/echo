// an extern function cannot be generic: one raw C symbol has no per-instantiation body. this is
// exactly why mem::alloc<T> is Echo code layered over a concrete alloc_bytes
extern {
    function malloc as bad_alloc<T>(usize $count) : ptr<T>;
}

echo 1;
