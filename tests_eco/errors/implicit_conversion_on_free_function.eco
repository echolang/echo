// which type a conversion is *from* is the type it is declared on, so a free function has no answer
struct Window { usize $count; }

#[implicit]
function window() : Window { return Window(1); }

echo 1;
