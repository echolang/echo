expect: fail
# an operator says it in its own words - the candidate list this used to print named `array<T>`,
# `map<K,V>` and `ordered_map<K,V>`, none of which this program has anything to do with. every
# `operator` declaration shares the root namespace, so that list is the same nine lines in every program

--- OUT --->
[error] NoMatchingOverload  index_no_element_contract.eco:6:8

  no overload of operator '[]' accepts a 'Plain' and a 'int32'. Declare one for it, or convert the operands first.

  5 | $s = Plain(1);
  6 | echo $s[0];
    :        ^^^^
  7 | 

1 error, nothing was compiled.
