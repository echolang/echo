// the write has to reach the caller's container. by value it lands in a copy that dies with the call, so
// the operator would register, be chosen, and silently do nothing
struct bag { ptr<int32> $d; }

operator (bag $b)[usize $i] = (int32 $v) : void
{
    $b->d:$[$i] = $v;
}
