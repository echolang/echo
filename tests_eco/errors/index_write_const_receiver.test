# and a const borrow is the same silent no-op from the other side

expect: fail

--- OUT --->
[error] index_write_const_receiver.eco:4:25

  an index-write operator takes its container as a mutable borrow - 'C&', not 'const bag&'. The write has to reach the caller's container: by value it would land in a copy that dies with the call, and through a 'const' borrow it would not land at all.

  3 | 
  4 | operator (const bag& $b)[usize $i] = (int32 $v) : void
    :                         ^
  5 | {

1 error, nothing was compiled.
