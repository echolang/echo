# an index-write operator is a statement, so 'void' is required rather than merely tolerated - the mirror
# of the refusal every other fixity gets for returning void

expect: fail

--- OUT --->
---- Issue ----
Issue at 5:19
an index-write operator is a statement, not an expression - '$c[$k] = $v' produces no value, so it returns 'void' and not 'int32&'. Declare the borrowing form 'operator (C& $c)[K $k] : V&' if you want a place a write goes through.
Begin: '['
End: '['
Code excerpt:
 [4]> 
 [5]> operator (bag& $b)[usize $i] = (int32 $v) : int32&
                        ^
 [6]> {

Critical issues found, cannot compile.
