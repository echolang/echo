# the value is an ordinary call argument now, so a wrong type is reported the way every other argument
# mismatch is - against the one operator the container declares, naming the operand that is wrong.
#
# it names the *value* and not the receiver because argument 0's bindings are authoritative in
# AST::can_instantiate: 'map<string, int32>' is a type the author declared, so V binds from it and the
# value is what has to fit. bound from the value instead, this said "accepts a 'map<string,int32>'" -
# true, and about the one operand that was right

expect: fail

--- OUT --->
---- Issue ----
Issue at 9:3
Invalid type conversion: no overload of operator '[]=' accepts a 'string' here - declare one for it, or convert the operand first.
Begin: 'operator []='
End: 'operator []='
Code excerpt:
 [8]> 
 [9]> $m['a'] = 'not an int';
        ^
 [10]> 

Critical issues found, cannot compile.
