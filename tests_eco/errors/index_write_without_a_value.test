# the value operand is not optional, so the write form's arity range starts one above the borrowing form's

expect: fail

--- OUT --->
[error] index_write_without_a_value.eco:5:19

  an index-write operator takes the container, then its indices, then the value it is given, e.g. 'operator (map<K, V>& $m)[const K& $key] = (V $value) : void'. Two operands is the append write '$c[] = $v'.

  4 | 
  5 | operator (bag& $b)[] = () : void
    :                   ^
  6 | {

1 error, nothing was compiled.
