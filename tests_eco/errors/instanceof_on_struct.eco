// a struct carries no runtime metadata, so there is nothing to read the answer from. only the left
// side is checked: a *class* tested against a struct type is a fair question that folds to false

struct Rock { int32 $mass; }

$r = Rock(1);
echo $r instanceof Rock;
