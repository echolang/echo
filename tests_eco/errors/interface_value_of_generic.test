expect: fail

--- OUT --->
[error] InvalidTypeConversion  interface_value_of_generic.eco:20:14

  Invalid type conversion: 'Bag<int32>' is a generic instantiation, and storing one as 'Sized<int32>' is not supported yet - a vtable needs a body per requirement, which a template only gets per call site. Take it through a constrained generic instead.

  19 | $bag = Bag<int32>(7);
  20 | Sized<int32> $erased = $bag;
     :              ^^^^^^^
  21 | 

1 error, nothing was compiled.
