modules: errors/lib_internal_surface
expect: fail

# **the default is the module**, so a consumer is refused every name the library did not write `public` on
# while its stated surface stays callable. deliberately also the case with **no label** on any refusal - a
# cross-module declaration resolves to no file through the module the diagnostic was built against
--- OUT --->
[error] InaccessibleDeclaration  internal_across_modules.eco:10:1

  'Detail' is internal to the module 'sealedlib', so the module 'main' cannot name it. Write 'public' on the declaration to make it part of what 'sealedlib' offers.

   9 | 
  10 | Detail $d = Detail(2);
     : ^^^^^^
  11 | echo detail_of($d->n) + DETAIL_SCALE;

[error] InaccessibleDeclaration  internal_across_modules.eco:11:25

  'DETAIL_SCALE' is internal to the module 'sealedlib', so the module 'main' cannot name it. Write 'public' on the declaration to make it part of what 'sealedlib' offers.

  10 | Detail $d = Detail(2);
  11 | echo detail_of($d->n) + DETAIL_SCALE;
     :                         ^^^^^^^^^^^^
  12 | 

[error] InaccessibleDeclaration  internal_across_modules.eco:10:13

  'Detail' is internal to the module 'sealedlib', so the module 'main' cannot name it. Write 'public' on the declaration to make it part of what 'sealedlib' offers.

   9 | 
  10 | Detail $d = Detail(2);
     :             ^^^^^^
  11 | echo detail_of($d->n) + DETAIL_SCALE;

[error] InaccessibleDeclaration  internal_across_modules.eco:11:6

  'detail_of(int32)' is internal to the module 'sealedlib', so the module 'main' cannot name it. Write 'public' on the declaration to make it part of what 'sealedlib' offers.

  10 | Detail $d = Detail(2);
  11 | echo detail_of($d->n) + DETAIL_SCALE;
     :      ^^^^^^^^^
  12 | 

4 errors, nothing was compiled.
