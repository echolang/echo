// a literal is typed from its destination, including a field reached through an element - so 300
// is out of range for the int8 it is written into
struct Small {
    int8 $b;
}

ptr<Small> $s = mem::alloc<Small>(1);
$s:$[0]->b = 300;
mem::free($s);
