// only a place can be moved *out of* - there has to be storage left behind for the move to empty.
// a call result already moves, so `mv` on one has nothing to do

function make() : int32
{
    return 1;
}

$a = mv make();
