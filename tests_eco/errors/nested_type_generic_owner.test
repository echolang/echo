expect: fail

--- OUT --->
[error] nested_type_generic_owner.eco:3:24

  'cursor' is declared inside the generic type 'Box', which is not supported yet - declare it alongside its owner instead.

  2 | // answer only while it cannot mention the owner's T, and nothing enforces that
  3 | struct Box<T> { struct cursor { usize $i; } T $v; }
    :                        ^^^^^^
  4 | 

1 error, nothing was compiled.
