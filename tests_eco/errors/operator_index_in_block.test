expect: fail

--- OUT --->
---- Issue ----
Issue at 6:5
An operator cannot be declared inside a block. Declare it at file scope - its symbol is visible to the whole program, so it cannot be scoped to one.
Begin: 'operator'
End: 'operator'
Code excerpt:
 [5]> {
 [6]>     operator (Bag& $b)[usize $i] : int32& { return &$b->at:$[$i]; }
          ^
 [7]> }

Critical issues found, cannot compile.
