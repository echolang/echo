expect: fail

--- OUT --->
---- Issue ----
Issue at 4:20
'++' cannot be declared as an operator: `$i++` is a statement, and it always means `$i = $i + 1`.
Begin: '++'
End: '++'
Code excerpt:
 [3]> // silently ignored at the only spelling of it that parses
 [4]> operator (int32 $a)++: int32 { return $a; }
                         ^
 [5]> 

Critical issues found, cannot compile.
