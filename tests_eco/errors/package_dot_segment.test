modules: packages/bad_dot_segment
expect: fail

--- OUT --->
[error] InvalidAttributeValue  module.eco:2:13

  'foo/../bar' is not a usable package name - it becomes a directory under 'vendor/', so each component must be a real segment (not '.', '..', empty, or a backslash).

  1 | #[module: "baddot"]
  2 | #[requires: "foo/../bar" { version: "^1", source: git "https://x" }]
    :             ^^^^^^^^^^^^
  3 | #[sources: "src/*.eco"]

