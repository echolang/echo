expect: fail

--- OUT --->
[error] TemporaryMember  pointer_member_of_temporary.eco:26:18

  'Buffer' has no storage of its own, so the pointer in its member 'data' would be an address into a value destroyed at the end of this statement.

  25 | $b = Box();
  26 | $q = $b->make()->data;
     :                  ^^^^
  27 | 

  help: Bind it to a variable first.

1 error, nothing was compiled.
