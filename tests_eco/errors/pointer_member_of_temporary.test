expect: fail

--- OUT --->
[error] pointer_member_of_temporary.eco:26:18

  'Buffer' has no storage of its own, so the pointer in its member 'data' would be an address into a value destroyed at the end of this statement. Bind it to a variable first.

  25 | $b = Box();
  26 | $q = $b->make()->data;
     :                  ^^^^
  27 | 

1 error, nothing was compiled.
