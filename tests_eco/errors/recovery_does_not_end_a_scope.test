expect: fail
# one diagnostic, and it is the only thing wrong with the file. the `return`'s own terminator check is
# skipped when its expression failed - the sub-parse already reported and already recovered, so asking
# about the terminator reports a missing ';' against the next statement's first token

--- OUT --->
[error] UnknownVariable  recovery_does_not_end_a_scope.eco:16:33

  The variable '$x' is not declared in the current scope

  15 | 
  16 | function f() : bar { return bar($x); }
     :                                 ^^
  17 | 

1 error, nothing was compiled.
