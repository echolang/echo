// a bool literal is not a number, so it does not fit a float return
// (tests_eco/functions/return_integer_widths.eco has the case where it does fit)

function not_a_number() : float64
{
    return true;
}

echo not_a_number();
