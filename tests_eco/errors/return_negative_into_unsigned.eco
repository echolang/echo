// a returned literal is typed against the declared return type, so it gets the same diagnostic
// `uint32 $x = -1;` gets rather than wrapping around

function negative() : uint32
{
    return -1;
}

echo negative();
