// returning a computed value where the signature promises a borrow.
// `return $x` of a by-value parameter is a dangling-local error (the
// wrap plants AddrOf; TypeChecker treats a by-value parameter as a
// local). a non-place still cannot answer T&
function bad(int $x) : int& {
    return $x + 1;
}

echo 1;
