// returning a value where the signature promises a borrow. this used to reach codegen and fail
// llvm's own verifier with "Function return type does not match operand type of return inst",
// which named neither the function nor the line
function bad(int $x) : int& {
    return $x;
}

echo 1;
