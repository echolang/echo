expect: ok

--- OUT --->
3
4
6
--- IR --->
// a struct's conformance costs nothing at runtime: there is no erased value and no
// per-struct vtable. (stdlib classes that conform to iterable still carry one.)
// each constrained call is monomorphized to a direct one
CHECK-NOT: eco.iface
CHECK-NOT: Triangle.vtable
CHECK-NOT: Square.vtable
CHECK-NOT: Hexagon.vtable
CHECK: define i32 @main(
