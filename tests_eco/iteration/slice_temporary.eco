// the source may be a temporary: `->sub()` hands back a value with no storage, and the loop's own
// iterator declaration is what gives it a slot for as long as the loop runs
$a = array<int32>();
$a[] = 1;
$a[] = 2;
$a[] = 3;
$a[] = 4;

foreach ($a->sub(1, 2) as $x) {
    echo $x;
}
