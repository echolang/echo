#ifndef ECO_TEST_SHIM_H
#define ECO_TEST_SHIM_H

/* reached only through the `include:` path, so this file being found at all is the assertion */
#define ECO_SHIM_BONUS 2

int eco_shim_answer(void);

#endif
