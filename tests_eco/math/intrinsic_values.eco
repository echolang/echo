// one call per family, checked for its value rather than its lowering. the point is that a
// declaration whose signature merely *satisfies* LLVM can still compute the wrong thing - a
// swapped argument order or a wrong overload would pass intrinsic_mangling and fail here

// trigonometry, at values whose results print exactly
float64 $sin = std::math::sin(0.0);
float64 $cos = std::math::cos(0.0);
float64 $tanh = std::math::tanh(0.0);

// atan2 takes y first, then x. atan2(1, 1) is an eighth of a turn, ~0.785398
float64 $atan2 = std::math::atan2(1.0, 1.0);

// powers and logarithms
float64 $pow = std::math::pow(2.0, 10.0);
float64 $exp2 = std::math::exp2(10.0);
float64 $log2 = std::math::log2(1024.0);
float64 $log10 = std::math::log10(1000.0);

// rounding. round() and roundeven() disagree on exact halves, which is the reason both exist
float64 $round = std::math::round(2.5);
float64 $roundeven = std::math::roundeven(2.5);
float64 $floor = std::math::floor(2.7);
float64 $ceil = std::math::ceil(2.1);
float64 $trunc = std::math::trunc(2.7);

// sign. copysign takes the magnitude first and the sign second
float64 $neg = 0.0 - 1.0;
float64 $copysign = std::math::copysign(3.0, $neg);
float64 $abs = std::math::abs($neg);

// min, max and the clamp built on top of them
float64 $min = std::math::min(3.0, 9.0);
float64 $max = std::math::max(3.0, 9.0);
float64 $clamp_high = std::math::clamp(9.0, 0.0, 5.0);
float64 $clamp_low = std::math::clamp(0.0 - 9.0, 0.0, 5.0);
float64 $clamp_in = std::math::clamp(2.0, 0.0, 5.0);

// fused multiply-add: a * b + c
float64 $fma = std::math::fma(2.0, 3.0, 4.0);

echo $sin;
echo $cos;
echo $tanh;
echo $atan2;
echo $pow;
echo $exp2;
echo $log2;
echo $log10;
echo $round;
echo $roundeven;
echo $floor;
echo $ceil;
echo $trunc;
echo $copysign;
echo $abs;
echo $min;
echo $max;
echo $clamp_high;
echo $clamp_low;
echo $clamp_in;
echo $fma;
