// heap storage outliving no scope in particular: allocate, index, free
// (book/concept/pointers_and_refs_v2.md)
ptr<int32> $p = mem::alloc<int32>(4);

$p:$[0] = 424242;
$p:$[1] = 696969;

echo $p:$[0];
echo $p:$[1];

mem::free($p);
