// a key type whose hash is a *constant*, so every key lands in one bucket. that is the only way to
// exercise probing, tombstone reuse and rehash under complete collision from a test
public struct clash
{
    int32 $n;
}

operator (const clash& $a) == (const clash& $b) : bool
{
    return $a->n == $b->n;
}
