stdlib: off
expect: fail

--- OUT --->
[error] array_literal_without_stdlib.eco:3:1

  an array literal with no declared type needs the core array type, and nothing in this program declares '#[core: "array"]'. it lives in the standard library, which this compilation left out - write the type, e.g. 'array<int32> $a = [...];'.

  2 | // `#[core: "array"]` lives in the standard library this compilation left out
  3 | $a = [1, 2];
    : ^^
  4 | 

1 error, nothing was compiled.
