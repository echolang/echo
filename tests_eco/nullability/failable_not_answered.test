expect: fail

# **the mistake is reported once, at the `struct`.** what this pins is that a `failable<E>` declared and
# not answered is discovered by AST::unwrap_plan_for - which carries the `failure()` declaration now -
# rather than by an unknown-function error inside a call AST::GuardLowering synthesized, and that the guard
# then says nothing of its own about it.
#
# the second error is the tail every refused guard has - see `unwrappable_not_declared` and
# `failable_not_declared`, which both carry the same one
--- OUT --->
[error] UnmetInterfaceRequirement  failable_not_answered.eco:27:8

  'cell' says it conforms to 'contract::failable<oops>' but does not satisfy 'failure() : oops&' - it declares no 'failure'.

  26 | 
  27 | struct cell : contract::unwrappable<int32>, contract::failable<oops>
     :        ^^^^
  28 | {

[error] failable_not_answered.eco:44:16

  'guard' needs a value that may be absent, and 'cell' always is one - write 'cell?' if it may not be, or drop the guard

  43 | 
  44 |     int32 $v = guard $c else ($e) { return $e->code; }
     :                ^^^^^
  45 | 

2 errors, nothing was compiled.
