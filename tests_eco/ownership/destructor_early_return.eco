// a `return` leaves every enclosing scope at once, so it owes the drops of all of them, innermost
// first

struct Buffer
{
    usize $tag;
    ptr<uint8> $data;

    destructor()
    {
        echo $this->tag;
        mem::free($this->data);
    }
}

function bail(bool $early) : usize
{
    $outer = Buffer(1, mem::alloc<uint8>(1));

    if ($early) {
        $inner = Buffer(2, mem::alloc<uint8>(1));
        return 10;
    }

    return 20;
}

echo bail(true);
echo bail(false);
