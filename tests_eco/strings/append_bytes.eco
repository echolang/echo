// append is append for a pointer and a length. a literal still goes through the gate

$src = 'xyz';

$lit = 'ab';
$lit->append($src->data(), $src->size());
echo $lit;

$empty = '';
$empty->append($src->data(), 0);
echo $empty->size();
echo $empty;

$empty->append($src->data(), 2);
echo $empty;
