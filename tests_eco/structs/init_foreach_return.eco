struct Box
{
    int32 $n;
    int32 $flag;

    constructor(int32 $n)
    {
        $this->n = $n;
        foreach (0 .. 1 as $i) {
            return;
        }
    }

    init
    {
        $this->flag = 7;
    }
}

Box $b = Box(1);
echo $b->flag;
