// a field default is a parameter default. Doc() clones [1, 2] into the call, and the argument
// destination types it the same way a written Doc([1, 2]) would

struct Doc
{
    array<int32> $body = [1, 2];
}

Doc $d = Doc();
echo $d->body->len;
echo $d->body[0];
echo $d->body[1];
