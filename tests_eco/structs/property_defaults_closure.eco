// a property default that holds a closure is published onto the file root in the body pass - the
// declaration pass has no root to publish to - and bind clones the expression while sharing the decl

struct Box
{
    function<int32()> $f = function() : int32 { return 7; };
}

Box $b = Box();
echo $b->f();
